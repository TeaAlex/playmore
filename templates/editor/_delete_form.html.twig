<form method="post" action="{{ path('editor_delete', {'id': editor.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ editor.id) }}">
    <input type="submit" class="text-red-light font-bold bg-transparent cursor-pointer" value="Supprimer">
</form>
