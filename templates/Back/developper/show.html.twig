<!DOCTYPE html>

<title>Developper</title>

{% block body %}
    <h1>Developper</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ developper.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ developper.name }}</td>
            </tr>
            <tr>
                <th>Logo</th>
                <td>{{ developper.logo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('developper_index') }}">back to list</a>

    <a href="{{ path('developper_edit', {'id': developper.id}) }}">edit</a>

    {{ include('developper/_delete_form.html.twig') }}
{% endblock %}
