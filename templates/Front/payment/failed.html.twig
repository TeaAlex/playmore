{% extends 'Front/base.html.twig' %}

{% block content %}


    <div class="w-full min-h-screen bg-grey-lightest">
        <div class="w-1/3 h-auto mx-auto px-8 py-16 pb-4">
            <h1 class="text-primary mb-6">Echec</h1>
            <div class="flex-col rounded bg-white shadow-md p-6">
                <p class="succes-msg"> Oups, il y'a un problème.</p>

                <p class="succes-text"> Il semblerait que le paiement n'a pas abouti. :(</p>
                <p class="succes-text"> Veuillez <a href="{{ path('app_security_payment') }}">ré-essayer</a>.</p>
                <a class="btn btn-secondary" href="{{ path('app_security_home') }}">Retour</a>
            </div>
        </div>
    </div>


{% endblock %}